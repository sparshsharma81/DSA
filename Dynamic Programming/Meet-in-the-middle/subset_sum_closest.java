// package Meet-in-the-middle;

public class subset_sum_closest {
    public static void main(String args[]){
        int[] a = {5,3,-1,7};
        int s= 17;
        //we need to generate a subset whose sum is closest to 17 but should not exceed that
        
        //subset are similar to subsequence..where each element has 2 choices
        //either include or not include 
        //total number of subsets -- 2^N
    }
}
